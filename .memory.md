# Ovnivers — Project Memory

## What is this
Addon para **Stremio** que combina torrent indexers (GloDLS + Nyaa.si) + embed resolver + 77 Alfa providers (48 activos) + Pigamer37 anime proxy + 61 Hermes scrapers (legacy, rotos) + 4 backend scrapers (rotos). Backend en **Render.com**.

---

## Architecture (v1.6.5)

```
Stremio App
  |-- Settings > Addons
  |     URL: https://ovnivers.onrender.com/manifest.json
  |     URL configurada: .../?configured=<base64>
  |
  |-- Backend: Render.com (Node.js / Express)
  |     |-- /manifest.json           → Stremio manifest (respeta config)
  |     |-- /stream/:type/:id.json   → Torrent indexers (GloDLS + Nyaa)
  |     |                              + Embed resolver (YouTube/JWPlayer)
  |     |                              + Backend scrapers (2embed+Mirrors, PoseidonHD)
  |     |                              + Alfa providers + 62 local providers + Pigamer37 proxy
  |     |-- /catalog/:type/:id.json  → TMDB catalogs (23 categorías: movies, series, anime)
  |     |-- /meta/:type/:id.json     → TMDb metadata + anime metadata
  |     |-- /configure               → Panel moderno con generate URL
  |     |-- /health                  → Provider-level stats (ok/fail/avgMs per provider)
  |     |-- /                        → Health check
  |
  |-- Proxy (v1.6.5): global.fetch override + bypass list
  |     |-- PROXY_URL env var → Cloudflare Worker (workers.dev) o HTTP proxy (https-proxy-agent)
  |     |-- TODAS las llamadas fetch() (server.js + providers + Alfa) pasan por el proxy
  |     |-- Cloudflare Worker: https://ovnivers-proxy.calipo10009.workers.dev
  |
  |-- Server-side provider execution:
  |     |-- Torrent indexers: GloDLS (cheerio) + Nyaa.si (cheerio)
  |     |-- Embed resolver: YouTube ytInitialPlayerResponse, JWPlayer setup
  |     |-- Backend scrapers: 2embed+Mirrors (8 mirrors), PoseidonHD (3 domains)
  |     |-- Alfa providers (77 registrados, 48 activos) via require()
  |     |-- 62 local providers (Hermes scrapers) via require()
  |     |-- Deduplicación: por url/infoHash + name
  |     |-- Límite: MAX_STREAM_RESULTS=80 por request
  |     |-- Timeout: LOCAL_PROVIDER_TIMEOUT=10s por provider
  |     |-- Concurrencia: LOCAL_PROVIDER_CONCURRENCY=6 providers en paralelo
  |     |-- Global timeout: STREAM_GLOBAL_TIMEOUT=18s (resultados parciales)
  |     |-- Embed resolution: 6s timeout, max 8 embeds
  |
  |-- Modo legacy (deshabilitado por defecto):
        exposeLocalScrapers=false → el campo "scrapers" NO se incluye en manifest
        Si se activa, Nuvio los descarga y ejecuta en Hermes (motor JS local).
```

---

## Key discoveries

### 1. Los scrapers ofuscados son para Hermes, NO para Node.js
- Los 61 archivos Hermes en `providers/` estan ofuscados con `__async`, `__defProp`, etc. (más 1 `alfa-providers.js` = 62 archivos total).
- Diseniados para **Hermes** (motor JS de React Native en Nuvio).
- **NO se pueden ejecutar en el backend.** Son para ejecucion local en el dispositivo.

### 2. Nuvio 0.7.5 usa protocolo Stremio
- Solo existe "Addons" (Stremio protocol), no "Local Scrapers".
- Campos obligatorios: `id`, `version`, `name`, `logo`, `resources`, `types`, `idPrefixes`.
- Formato de resources: array de objetos `[{name, types, idPrefixes}]`.

### 3. El campo "scrapers" en el manifest
- Si se incluye, Nuvio los ejecuta **localmente en el dispositivo**.
- Se puede omitir via config (`enableLocal: false`).
- Se envia condicionalmente en el manifest segun `?configured=`.

### 4. APIs de streaming bloquean IPs de servidores (INVESTIGADO 2026-06-11)

**Resultado del test masivo:** practicamente NINGUNA API de movies/TV devuelve JSON desde Render.

| Provider | URL | Resultado |
|---|---|---|
| streamsrcs.2embed.cc (vesy) | `/vesy?tmdb=550` | HTML (pagina player, no JSON) |
| streamsrcs.2embed.cc (vsrc) | `/vsrc?imdb=tt0137523` | HTML |
| www.2embed.skin | `/api/movie/550.json` | null (bloqueado) |
| vidsrc.rip | `/api/movie/550` | fetch failed |
| vidsrc.to | `/api/v2/movie/550` | 404 |
| vidsrc.pro | `/api/movie/550` | aborted |
| vidsrc.cc | `/api/movie/550` | aborted |
| vidsrc.stream | `/api/movie/550` | fetch failed |
| vidsrc.net | `/api/movie/550` | fetch failed |
| vidsrc.icu | `/api/movie/550` | fetch failed |
| vidsrc.xyz | `/api/movie/550` | fetch failed |
| vidsrcme | `/api/movie/550` | fetch failed |
| vidlink.pro | `/api/movie/550` | 403 |
| embed.su | `/api/movie/550` | fetch failed |
| vidcloud.icu | `/api/movie/550` | 404 |
| superembed | `/api/movie/550` | fetch failed |
| vidbinge.dev | `/api/movie/550` | HTML placeholder (no JSON) |
| moviesapi.club | `/movie/550` | fetch failed |
| multiembed.mov | `/api/movie/550` | 404 |
| autoembed.co | `/api/movie/550` | 404 |
| vidplay.site | `/api/movie/550` | 404 |
| player.videasy.net | `/api/movie/550` | 404 |

**Conclusion:** TODAS las APIs de peliculas/series estan muertas o bloqueadas desde servidores. 2embed.cc devuelve HTML con proteccion anti-devtool, el stream real requiere seguir una cadena de iframes.

**Workaround (v1.2.0):** `parse2embedHTML()` extrae m3u8/mp4/iframe/data-src URLs del HTML de 2embed.cc mediante 6 patrones regex. Funciona parcialmente.

**Unico que funciona:** `pigamer37.alwaysdata.net` para anime (alwaysdata.net no bloquea IPs). TMDB API tambien siempre funciona.

### 5. Pigamer37 como proxy de anime
- El addon original de Pigamer37 usa `|` (pipe) en IDs de catalogo: `animeflv|onair`, `animeflv|search`
- Content IDs usan `:` (colon): `animeflv:slug`, `animeav1:slug`
- Soporta: AnimeFLV, AnimeAV1, TioAnime, Henaojara
- IDs: `animeflv:`, `animeav1:`, `henaojara:`, `tioanime:`, `anilist:`, `kitsu:`, `mal:`, `anidb:`
- **IMPORTANTE:** Pigamer37 espera IDs con `%7C` (pipe encodeado), NO `%3A` (colon encodeado). `encodeURIComponent` encodea `:` como `%3A` — hay que usar `fixPigamerId()` que reemplaza `:` por `%7C`.

### 6. Stremio stream format (Pigamer37 reference)
- `name`: `"SourceName\nServerName"` (multilinea con `\n`)
- `title`: `"Episode Title\n⚙️ ServerName\n🔗 url\n🇯🇵🇪🇸"` (info detallada)
- `behaviorHints.notWebReady: true` para streams internos
- `behaviorHints.bingeGroup`: `"source|server"` para autoplay
- `externalUrl` (sin `url`) para streams que abren en navegador
- `proxyHeaders` para headers custom al fetchear el video

### 7. Stremio catalog format (Pigamer37 reference)
- Catalogs con `extra: [{ name: "genre", options: [...], optionsLimit: 1 }]` para generos
- `extra: [{ name: "search", isRequired: true }]` para busqueda
- `skip` para paginacion: `page = Math.floor(skip / pageSize) + 1`
- Genre catalog sin search: `id: "animeflv|genres"`, `extra.genre.isRequired: true`
- Los `idPrefixes` deben incluir todos los prefixes usados en items (incluyendo `tmdb-genre:`)

---

### 8. DonTorrent: Doble PoW (Anubis + API interno) + .torrent download

DonTorrent tiene un sistema de protección de 2 capas:

**Capa 1 - Anubis (Cloudflare):** PoW SHA256 con dificultad 5 (~50K-1M iteraciones). Se resuelve en `bypassAnubisChallenge()` en engine.js. Cookie `browser-pow-auth` cacheada por dominio.

**Capa 2 - PoW interno (`/api_validate_pow.php`):**
- `POST { action: "generate", content_id, tabla }` → recibe `challenge` (SHA256 hex)
- Resolver: SHA256(challenge + nonce) con 3 ceros hex leading (~3K iteraciones, ~7ms)
- `POST { action: "validate", challenge, nonce }` → recibe `download_url` (protocol-relative URL al .torrent)
- Descargar .torrent → parsear bencode → extraer infoHash (SHA1 del bloque `info`)

**Click handler JS (`don_download.js`):**
```javascript
// 1. POST /api_validate_pow.php { action: 'generate', content_id, tabla }
// 2. computeProofOfWork(challenge, difficulty=3) — SHA256(challenge+nonce), leading zeros
// 3. POST /api_validate_pow.php { action: 'validate', challenge, nonce }
// 4. window.location.href = result.download_url
```

**URL estructural:**
- Películas: `/pelicula/{content_id}/{slug}`
- Series (temporada): `/serie/{show_id}/{season_id}/{slug}`
- Tabla de episodios: `<table.table>` con filas `<tr>` → `1x01 - Titulo`, botón `.protected-download` con `data-content-id` y `data-tabla="series"`, clave "Sin Contraseña"
- Búsqueda: solo POST a `/buscar` con `valor={query}`, NO GET

**Streams resultantes:** .torrent file → infoHash extraído → stream con `{ url: dl_url, infoHash, filename, sources: ['dht:'+infoHash] }`

## Fixes aplicados (2026-06-15)

### ✅ ADDED — Torrent indexers: GloDLS + Nyaa.si
- **GloDLS** (movies/TV): scraper cheerio desde `glodls.to/search_results.php`. 15 resultados con seeds, calidad, infoHash. Fix `cat=0` (rompía búsqueda). Fix selectores `t-row`, columnas name=col1/size=col4/seeds=col5. Fix name extraído de `a[title]`.
- **Nyaa.si** (anime): scraper cheerio desde `nyaa.si`. 75 resultados con seeds, calidad, infoHash.
- **Integración en server.js**: Torrent indexers en pipeline tras Alfa/Hermes. TMDB title fetch para búsqueda por nombre real. Soporte `S01E01` para TV.

### ✅ ADDED — Embed resolver domain-specific
- **`src/alfa-providers/embed-resolver.js`**: `resolveEmbed(embedUrl)` con per-domain handlers. YouTube extrae `ytInitialPlayerResponse` → `streamingData.formats` (URL directa Google Video). JWPlayer extrae `setup({file: ...})`. Genérico busca src/file keys.
- **Integración**: engine.js `tryResolveEmbedToDirect()` ahora delega a `embed-resolver.js`.

### ✅ FIXED — Provider audit results
- **Hermes scrapers (62)**: TODOS rotos — bloqueados, devuelven 0, o JS-dependent.
- **Backend scrapers (4)**: TODOS rotos — 2embed APIs HTML, VidSrc fails, PoseidonHD Next.js.
- **Torrent indexers**: 2 funcionales — GloDLS + Nyaa.si. Primeros providers que devuelven infoHash real desde Render.

### ✅ FIXED — Alfa providers bundle no se cargaba en producción (CRÍTICO)
- **BUG RAÍZ:** `providers/alfa-providers.js` exporta directo como función (`module.exports = scrapeAlfaProviders`), pero `server.js` línea 67 hacía `require('./providers/alfa-providers').default` que devuelve `undefined`. Todos los Alfa providers (47 "activos") **nunca se ejecutaban**.
- **Fix aplicado:** `server.js` ahora usa `alfaModule.default || alfaModule` para soportar ambos formatos de export (directo o esbuild `{__esModule, default}`).
- **Impacto:** CineCalidad, PelisPedia, HomeCine ahora devuelven streams. Antes: 0.

### ✅ FIXED — Selectores de búsqueda desactualizados
- **CineCalidad:** `div.grid` → `article`, `a[href]` → `h2`. Ahora encuentra "Matrix" correctamente.
- **AllCalidad:** URL de búsqueda incorrecta (`/search?s=` → `/?s=`), itemSelector `article.movie-item` → `article`.
- **DonTorrent:** Marcado `active: false` — dominio funciona pero bloquea con Cloudflare.
- **HDFull:** Marcado `active: false` — dominio devuelve 403.

### ✅ FIXED — Base64 data-src no se decodificaba
- **BUG:** CineCalidad y otros providers codifican las URLs de video en `data-src` como base64 (ej. `aHR0cHM6Ly92aW1lb3MubmV0...`). El extractor buscaba URLs que empiezan con `http` y se saltaba las encodeadas.
- **Fix aplicado:** Nueva función `resolveUrl()` en `engine.js` que detecta strings base64 (20+ chars alfanuméricos con `+/=`) y los decodifica antes de usarlos. También se agregó fallback a `<a data-src>` cuando no se encuentran iframes con URLs válidas.

### ✅ FIXED — CineCalidad container selector incorrecto
- **BUG:** `containerSelector: '.players'` no existe en el HTML de CineCalidad. Los players son `<a data-src="base64">` fuera de cualquier contenedor específico.
- **Fix aplicado:** Cambiado a `containerSelector: 'body'` para que busque en toda la página.

### Estado post-fix (tests locales)

| Categoría | Antes | Después |
|---|---|---|
| Movie: The Matrix (Alfa providers) | 0 streams | **12 streams** (CineCalidad 5, HomeCine 4, PelisPedia 3) ✅ |
| Series: Breaking Bad | 0 streams | **1 stream** (HomeCine trailer) ⚠️ |
| Anime: One Piece (Alfa) | 0 streams | **5 streams** (JKAnime) ✅ |
| Anime: One Piece (Pigamer37 vía Render) | Funcional | ✅ sin cambios |

**Pendiente:** Series providers siguen sin devolver streams reales (solo trailers). Los selectores de sitios de series (FullSerieHD, SeriesRetro, etc.) necesitan revisión similar.

### Estado v1.6.3 — Proxy bypass + timeout fixes

| Cambio | Detalle |
|---|---|
| `PROXY_URL` env var | Todas las llamadas `fetchAPI()` se enrutan por proxy (Cloudflare Worker o HTTP proxy via `https-proxy-agent`) |
| `node-fetch@2` polyfill | Para versiones de Node que no tienen `fetch` global (Render free tier) |
| `LOCAL_PROVIDER_TIMEOUT` | Reducido de 60s → 10s |
| `STREAM_GLOBAL_TIMEOUT` | Nuevo: 18s timeout global, devuelve resultados parciales |
| Embed resolution timeout | Limitado a 6s, max 8 embeds (antes 12) |
| Cache key | Ahora incluye config hash (quality, langs, backend/local flags) |
| Cache empty | Ya no cachea resultados vacíos (`unique.length > 0`) |
| Configure page JS | Rewrite con error handling, `window` globals, null safety |
| `parse2embedHTML` | Añadido `isValidEmbedUrl` filter + mirror name en labels |

### ✅ FIXED — v1.6.5: Proxy no llegaba a los providers (CRÍTICO)

- **BUG RAÍZ:** `PROXY_URL` solo se usaba en `fetchAPI()` de server.js. Los 62 providers y Alfa usan `fetch()` global directamente → sus requests iban sin proxy → IP de Render bloqueada por todos los sitios → 0 streams.
- **Fix:** `global.fetch` override al inicio de server.js. Si `PROXY_URL` está seteado, TODAS las llamadas `fetch()` (server.js, providers, Alfa) se enrutan por el proxy.
- **Cloudflare Worker:** URL rewrite `?url=...` para cada request.
- **HTTP proxy:** `HttpsProxyAgent` inyectado en cada `fetch()`.
- **fetchAPI simplificado:** Ya no tiene lógica de proxy duplicada (global.fetch lo maneja).
- **Resultado:** 15/52 providers ahora completan exitosamente desde Render (antes 0). Health endpoint muestra stats por provider.

### ✅ FIXED — v1.6.5: Timeout global descartaba todos los resultados

- **BUG RAÍZ:** `Promise.race([Promise.allSettled(streamTasks), timeout])` — cuando el timeout ganaba, `rawStreams` quedaba vacío. El log decía "returning partial results" pero devolvía `[]`.
- **Fix:** Resultados se acumulan incrementalmente en `rawStreams` conforme cada task completa. El timeout solo corta la espera, no descarta lo ya recolectado.

### ✅ FIXED — DonTorrent reactivado con PoW solver (2026-06-14, v1.5.2)
- **Anubis**: PoW SHA256 (dificultad 5) resuelto en `bypassAnubisChallenge()` — ya existente en engine.js
- **PoW interno**: Nuevo `solveSha256PoW()` en engine.js — SHA256(challenge + nonce), dificultad 3 (~3000 iteraciones)
- **API flow**: `downloadDontorrentTorrent()` — generate → solve PoW → validate → download .torrent → parse infoHash
- **POST search**: `searchProvider` ahora soporta `method: 'POST'` con body configurable
- **TV show episodes**: `getEpisodeUrl` type `dontorrent` — parsea tabla de episodios, con fallback que busca otras temporadas si el episodio no está en la página actual
- **Movie**: `extractVideos` type `dontorrent` — extrae contentId del botón `.protected-download`, ejecuta PoW flow, devuelve stream con infoHash
- **Resultados**: Matrix ✅, Breaking Bad S01E01 ✅, Breaking Bad S02E01 ✅ (diferente temporada vía fallback search)

### ✅ FIXED — MejorTorrent y PelisPanda reactivados (2026-06-14, v1.5.1)
- **MejorTorrent**: Dominio actualizado a `www43.mejortorrent.eu`, categorías `movie/tvshow/torrent`
- **PelisPanda**: Dominio actualizado a `pelispanda.org`, reactivado con streams torrent funcionales
- **DonTorrent**: Cambiado de `active: false` a config con Anubis solver + PoW download flow

## Current state (v1.6.5)

**Proxy:** `global.fetch` override con **bypass list** — 11 dominios Anubis (DonTorrent, MejorTorrent, etc.) van directo sin proxy. El resto por Cloudflare Worker (`ovnivers-proxy.calipo10009.workers.dev`). Worker v2 con cookie jar por dominio, header forwarding, retry con backoff.

**Torrent indexers:** 6 fuentes — GloDLS, Nyaa.si, SolidTorrents, LimeTorrents, 1337x, EZTV. LimeTorrents y 1337x ahora resuelven magnet/infoHash desde pagina de detalle. 81 torrents para "Fight Club" en test local.

**Embed resolver:** `src/alfa-providers/embed-resolver.js` con resolvers por dominio — YouTube (ytInitialPlayerResponse → Google Video URL directa), JWPlayer (setup inline). Integrado en engine.js.

**Backend scrapers:** 2embed+Mirrors (8 mirrors) + PoseidonHD (3 domains). 2embed devuelve 1 stream roto (`/embed/` sin video real). PoseidonHD devuelve 0.

**Local providers (62):** 15 completan exitosamente (4KHDHub, CineMM, CorsaroViola, Lordflix, MovieBlast, Movies4u, Movix, Nakios, PlayIMDb, StreamFlix, TopCartoons, Torrentio, MultiVid, VegaMovies, VidEasy). ~37 fallan (timeout, Cloudflare challenge, o dominio bloqueado). ~10 son anime-only (sin probar con contenido anime).

**Alfa providers:** 1 intento, fallo (10302ms timeout). Probablemente Cloudflare challenge en los sitios objetivo.

**Pigamer37 anime proxy:** Funciona (alwaysdata.net no bloquea IPs).

**Manifest dinamico:** Generado en `/manifest.json` con 19 catalogos TMDB + 4 universales + 4 Amatsu anime. Respeta configuracion de usuario.

**Health endpoint:** `/health` muestra stats por provider (total, ok, fail, failStreak, avgMs, lastOk, lastFail).

### Changelog v1.6.5

| Cambio | Detalle |
|---|---|
| **Smart Proxy Worker v2** | Cookie jar por dominio, header forwarding (Accept-Language, etc.), retry con backoff (3 intentos), health endpoint |
| **Proxy bypass list** | 11 dominios Anubis ruteados directo (sin proxy): DonTorrent, MejorTorrent, GranTorrent, DivXTotal, WolfMax4K, PelisPanda, EliteTorrent, MiTorrent, HackTorrent, BlogHorror. Configurable via `PROXY_BYPASS_DOMAINS` env var |
| **tt ID title resolution** | IDs `tt0137523` ahora resuelven titulo via TMDB find API para busqueda de torrents (antes usaba numero crudo) |
| **LimeTorrents + 1337x magnet** | Resuelven magnet/infoHash desde pagina de detalle (top 5). Antes devolvian `infoHash: ''` y se descartaban |
| **Torrent indexers** | 6 fuentes activas: GloDLS, Nyaa.si, SolidTorrents, LimeTorrents, 1337x, EZTV. 81 torrents para Fight Club en test local |
| **Total streams (Fight Club)** | **1** (roto) |

**Problema pendiente:** 15 providers completan pero devuelven 0 streams (sitios con Cloudflare challenge, selectores desactualizados, o contenido no disponible). El timeout global de 18s es muy corto — solo ~12 providers se procesan antes del corte.

### Test results anteriores (2026-06-12 — local desde Render, pre-fix)

| Categoria | Accesibles | Bloqueados |
|---|---|---|
| Backend scrapers | 3/4 activos | VidSrc parcial |
| Alfa Movies | 28/43 (65%) | 15 bloqueados |
| Alfa Series | 5/6 (83%) | 1 bloqueado |
| Alfa Anime | **10/22 (45%) funcionales** | 12 inactivos |
| Alfa Documentales | 3/4 (75%) | 1 bloqueado |
| **Total alfa providers** | **46/84 (55%)** | **38 inactivos** |

---

## Files structure

```
ovnivers/
  server.js            → Express backend (main for Render), 1758 líneas
    manifest.json        → Stremio manifest + 61 scrapers Hermes + 1 Alfa bridge (archivo de datos; servidor usa manifest dinámico)
  package.json         → cheerio, crypto-js, esbuild, express
  render.yaml          → Render deploy config
  railway.json         → Railway deploy config (alternative)
  build.js             → esbuild bundler
  providers/           → 61 scrapers Hermes (legacy, rotos) + alfa-providers.js bridge
   src/alfa-providers/  → Source for alfa-providers scraper
     index.js           → Main entry: resolveTitle + scrapeAlfaProviders
     engine.js          → fetchHTML, searchProvider, extractVideos, detectServer, PoW solvers
      providers.js       → 77 provider configs (48 activos: URLs, selectors, extraction types, POST search, dontorrent type)
   tests/               → Test scripts for providers and scrapers
  logo.png             → Addon icon
  .gitignore           → Excludes node_modules, .memory.md
  .memory.md           → This file
  README.md            → Project documentation

Nota: providers/ se cargan en server.js via require() para ejecución server-side.
Los scrapers Hermes que fallan al cargar se omiten silenciosamente (try/catch).
```

---

## Dependencies

| Package | Version | Purpose |
|---|---|---|
| express | ^4.21.0 | HTTP server, Stremio protocol |
| cheerio | ^1.2.0 | HTML parsing |
| cheerio-without-node-native | npm:cheerio@^1.2.0 | Alias for Hermes scrapers |
| crypto-js | ^4.2.0 | AES/SHA decryption |
| dotenv | ^16.6.1 | Environment variables (.env loader) |
| esbuild | ^0.25.0 | Bundler for local scrapers |
| node-fetch | ^2.7.0 | fetch polyfill for Node.js <18 (Render free tier) |
| https-proxy-agent | ^7.0.0 | HTTP/SOCKS proxy agent for fetch |

---

## Deployment

### Render.com
- **Service:** ovnivers (Web Service)
- **Plan:** Free (512MB RAM, cold start ~30s)
- **Repo:** leonidas10009/ovnivers
- **Build:** `npm install` → `npm run build`
- **Start:** `node server.js`
- **URL:** https://ovnivers.onrender.com
- **Auto-deploy:** enabled (pushes to `main`)

---

## URLs

| Resource | URL |
|---|---|
| GitHub repo | https://github.com/leonidas10009/ovnivers |
| Render backend | https://ovnivers.onrender.com |
| Manifest | https://ovnivers.onrender.com/manifest.json |
| Configure | https://ovnivers.onrender.com/configure |

---

## Config protocol

El panel `/configure` genera una URL con los ajustes codificados en base64:

```
https://ovnivers.onrender.com/manifest.json?configured=<json_base64>
```

El backend decodifica `configured` y:
1. Filtra `catalogs`, `resources`, `types`, `idPrefixes` en el manifest
2. Omite `scrapers` si `enableLocal: false`
3. Filtra streams por `quality` en `/stream`
4. Bloquea requests de tipos deshabilitados

Campos configurables:
```json
{
  "enableMovies": true,
  "enableSeries": true,
  "enableAnime": true,
  "quality": "all",
  "langs": ["en", "es", "ja", ...],
  "enableBackend": true,
  "enableLocal": true
}
```

---

## Known issues (audit 2026-06-12)

### ✅ FIXED — alfa-providers no funciona para anime ni documentales
- **Fix aplicado:** filtro cambiado a `p.categories.includes(category)` con mapeo `movie→movie, series→tvshow, anime→anime, other→documentary`

### ✅ FIXED — Anime IDs no se resuelven en alfa-providers
- **Fix aplicado:** para IDs con prefix anime, extraer slug directo sin pasar por TMDB

### ✅ FIXED — Pigamer37 IDs encodeados mal
- **Fix aplicado:** `fixPigamerId(id)` usado en stream, catalog y meta

### ✅ FIXED — Backend Cuevana2/PoseidonHD: `s + 0.2` no asigna
- **Fix aplicado:** cambiado a `s += 0.2`

### ✅ FIXED — Catalog genre items usan `tmdb-genre:` sin declarar en idPrefixes
- **Fix aplicado:** `"tmdb-genre:"` agregado a manifest y server

### ✅ FIXED — `type === 'anime'` fallback demasiado amplio
- **Fix aplicado:** solo `if (isAnimeId(id))` sin el `|| type === 'anime'`

### ✅ FIXED — Cache sin limites de tamaño
- **Fix aplicado:** LRU eviction con max 1000 entradas

### ✅ FIXED — TMDB API key hardcodeada
- **Fix aplicado:** `process.env.TMDB_KEY` con fallback

### ✅ FIXED — 24 providers muertos marcados inactive
- **Fix aplicado:** test masivo desde Render, 24 bloqueados marcados `active: false`

### ✅ FIXED — Alfa providers require devolvía `{__esModule, default}` en vez de la función (2026-06-13)
- **Fix aplicado:** `require('./providers/alfa-providers')` ahora usa `.default` porque esbuild genera `{__esModule: true, default: fn}`. Antes el código chequeaba `typeof scrapeAlfaProviders !== 'function'` → daba true → nunca se ejecutaban los 80+ scrapers.

### ✅ FIXED — Alfa providers selectores desactualizados (2026-06-13)
- **Fix aplicado:** `providers.js` regenerado desde `providers-fixed.js` con selectores actualizados. 35 providers funcionales (vs 0). Nuevo tipo `iframe-chain` para resolución de embeds anidados (pelispedia: tabs → iframe data-src → embed page → iframe src real).

### ✅ FIXED — Torrentio no tenía español (2026-06-13)
- **Fix aplicado:** Añadido al manifiesto como scraper Hermes (`en`, `hi`). Solo disponible si el usuario activa `exposeLocalScrapers`.

### ✅ FIXED — filterStreams permitía streams sin bandera de idioma (2026-06-13, v1.4.20)
- **Fix aplicado:** cuando el usuario configura idiomas específicos (es, cast, lat, ja), los streams sin bandera (backend scrapers en inglés) ahora se bloquean. Antes pasaban todos.

### ✅ FIXED — Trackers de torrents en formato inválido (2026-06-13, v1.4.21)
- **Fix aplicado:** trackers de magnet links se prefixeaban con `tracker:` (ej. `tracker:udp://...`) en vez de usar la URL real. Stremio/WebTorrent no reconocía el formato. Ahora se pasan como URLs reales (`udp://`, `https://`).

### ✅ ADDED — Type emoji prefixes (2026-06-13, v1.4.22)
- **Feature:** streams now show 📺 (direct), 🧲 (magnet torrent), 🔗 (.torrent file) prefix for visual type differentiation.
- **Fix aplicado:** test masivo desde Render, 24 bloqueados marcados `active: false`

### ✅ FIXED — Backend EZTV y Cuevana2 removidos
- **Fix aplicado:** no funcionan desde Render (403/fetch failed), quitados del array activo

### ✅ FIXED — 2embed devuelve HTML, no JSON → scrapers no extraen streams (2026-06-12)
- **Fix aplicado:** `parse2embedHTML()` extrae m3u8/mp4/iframe/data-src/json-block URLs via regex
- `scrape2embedVesy` y `scrape2embedVsrc` ahora detectan `typeof data === 'string'` y usan el parser HTML
- 6 patrones regex: m3u8, mp4, iframe src, JS assignments, data-src attributes, JSON blocks

### ✅ FIXED — Stremio manda extra params en URL path, no en query string (2026-06-12)
- **Fix aplicado:** rutas catch-all `/:type/:id/:extra(*).json` para stream, catalog y meta
- `parsePathExtras()` convierte `genre=28&skip=20` del path en `req.query`
- Handlers refactorizados a funciones `handleStream()`, `handleCatalog()`, `handleMeta()`

### ✅ FIXED — Anime catalogs usan type incorrecto (2026-06-12)
- **Fix aplicado:** todos los catalogs de anime cambiados a `type: "series"` (manifest.json + server.js)
- Coincide con Pigamer37 y Stremio espera `series` para contenido episodico

### ✅ FIXED — Genre catalogs no tienen `options` en manifest extra (2026-06-12)
- **Fix aplicado:** `loadGenres()` carga generos TMDB (movie + tv) con cache 24h
- Manifest genera `extra: [{ name: "genre", options: [...], optionsLimit: 1 }]` dinamicamente
- Fallback estatico en manifest.json con lista completa de generos

### ✅ FIXED — El addon no aparece como provider en catalogs de otros addons (2026-06-12)
- **Fix aplicado:** 2embed scrapers ahora devuelven streams reales (HTML parser)
- Manifest ya tenia `tt` en stream idPrefixes — el problema era que los scrapers no devolvian nada

### ✅ FIXED — Catalogo "By Year" no funciona sin search param (2026-06-12)
- **Fix aplicado:** `isRequired: true` en manifest + fallback a `new Date().getFullYear()` en server

### ✅ FIXED — `fixPigamerType()` y `cheerio` fallback son codigo muerto (2026-06-12)
- **Fix aplicado:** `fixPigamerType()` eliminado. `scrapeEZTV`, `scrapeCuevana2Espanol`, `getTitle` eliminados
- `scrapePoseidonHD` refactorizado para usar `getTMDbMeta()` directamente

### ✅ FIXED — Pigamer37 double-encoding bug (2026-06-12)
- **Fix aplicado:** `fixPigamerId()` ahora retorna `|` literal (no `%7C`), `encodeURIComponent` lo convierte a `%7C`
- Antes retornaba `%7C` literal → `encodeURIComponent` encodeaba `%` → `%257C` (doble encoding)

### ✅ FIXED — Pigamer37 ID encoding: `:` vs `|` confusion (2026-06-12, v1.2.4)
- **BUG RAÍZ de todo el fallo de anime:** `fixPigamerId()` convertía `:` → `|` para TODOS los IDs
- **Realidad:** Pigamer37 usa `|` solo en catalog IDs (`animeflv|onair`), pero `:` en stream/meta IDs (`animeflv:one-piece`)
- `encodeURIComponent` encodea `:` como `%3A` y `|` como `%7C`
- Ovnivers enviaba `animeflv%7Cone-piece` → Pigamer37 devolvía `{"streams":[],"message":"Failed getting Anime info"}`
- **Fix:** `fixPigamerId()` ahora es no-op (`return id`). `encodeURIComponent` maneja la codificación correcta automáticamente
- Catalog IDs con `|` se encodean a `%7C` correctamente. Stream/meta IDs con `:` se encodean a `%3A` correctamente
- **Verificado:** `animeflv%3Aone-piece` → Pigamer37 devuelve 30+ streams reales

### ✅ FIXED — Anime catalog detection con IDs sin colon (2026-06-12)
- **Fix aplicado:** `isAnimeId(cleanId + ':')` en handleCatalog para IDs como `animeflv` (sin `:`)

### ✅ FIXED — Anime cross-reference IDs no se resuelven (2026-06-12)
- **Fix aplicado:** `resolveAnimeId()` consulta `/meta` de Pigamer37 para convertir `kitsu:`, `mal:`, `anilist:`, `anidb:` a IDs fuente (`animeflv:`, etc.)
- Stream handler ahora resuelve IDs antes de proxy a Pigamer37
- Esto permite que el addon aparezca como provider cuando otros addons (Anime Kitsu) devuelven IDs cross-reference

### ✅ FIXED — idPrefixes incorrectos por recurso (2026-06-12)
- **Fix aplicado:** `stream` ya no incluye `tmdb-genre:` (no tiene sentido para streams)
- `catalog` ya no incluye `tt` (no tiene sentido para catalogs)
- Cada recurso tiene sus idPrefixes especificos

### ✅ FIXED — Compatibilidad con Stremio (2026-06-12)
- **Fix aplicado:** `types` incluye `"other"` para compatibilidad con addons que usan ese tipo
- `behaviorHints.newEpisodeNotifications: true` para notificaciones de episodios
- `idPrefixes` por recurso limpios y correctos

### ✅ ADDED — Server-side provider execution + deduplication (2026-06-12)
- **Integración providers:** Alfa providers + scrapers Hermes cargados via `require()` y ejecutados en el servidor
- **Deduplicación:** `dedupeStreams()` elimina streams duplicados por url/name/infoHash antes de cachear
- **Helpers nuevos:** `normalizeStream()`, `withTimeout()`, `runInChunks()`, `scrapeLocalProviders()`, `scrapeAlfa()`
- **Config keys nuevas:** `enableCatalogs` (default: true), `exposeLocalScrapers` (default: false)
- **ID parsing:** `parseStreamId()` extrae contentId/season/episode de IDs `tmdb:123:1:1` o `tt123:1:1`
- **Genre format:** Cambiado a `"id: name"` (ej. `"28: Action"`) con `parseGenreId()` que extrae el ID numérico
- **Límites configurables:** `MAX_STREAM_RESULTS=80`, `LOCAL_PROVIDER_TIMEOUT=12s`, `LOCAL_PROVIDER_CONCURRENCY=8`

### ✅ FIXED — Codex issues corregidos (2026-06-12)
- **Genre format:** `parseGenreId()` ahora se usa en `handleCatalog` para extraer el ID numérico del formato `"28: Action"`
- **Tipo 'other':** Re-agregado a `enabledTypes` en manifest (se había eliminado en la refactorización)
- **handleCatalog:** Refactorizado para usar `isTypeEnabled()` y `parseGenreId()`
- **handleMeta:** Refactorizado para usar `isTypeEnabled()`
- **Console.log:** Limpiado double log en handleStream

### ✅ FIXED — v1.2.6: Anime Alfa/Local providers no aparecían (2026-06-12)
- **BUG RAÍZ:** `scrapeAlfa()` se llamaba con `type='series'` → `mapTypeToCategory('series')` devolvía `'tvshow'`, pero los providers de anime están en categoría `'anime'`. Alfa providers buscaban en categoría incorrecta y devolvían vacío.
- **Fix:** `scrapeAlfa()` ahora recibe `type='anime'` para anime IDs, buscando en la categoría correcta.
- **BUG RAÍZ 2:** `scrapeLocalProviders()` recibía `rawId='animeflv:slug'` que no es un TMDB ID válido, los scrapers Hermes no encontraban nada.
- **Fix:** Nueva función `getAnimeTMDbId()` que consulta `/meta/series/` de Pigamer37 para resolver un TMDB ID. Si lo encuentra, lo pasa a `scrapeLocalProviders()`.

### ✅ FIXED — v1.2.6: Stream format inconsistente (2026-06-12)
- **BUG:** `normalizeStream()` sobrescribía `title` de Pigamer37 con `"Quality\nPigamer37"`, perdiendo episodio, servidor y flags de idioma.
- **Fix:** streams con `name` multiline se preservan intactos (incluyendo su `title` original). Solo streams con `name` simple se formatean a `"Provider\nQuality"`.

### ✅ ADDED — v1.2.6: Combined search catalog (2026-06-12)
- **FEATURE:** Nuevo catálogo `all-search` (type: series) que busca simultáneamente en TMDB + AnimeFLV + AnimeAV1 + TioAnime + Henaojara.
- Resultados mergeados y deduplicados por nombre, top 50.
- Respeta config: usa TMDB si `enableBackend=true`, anime sources si `enableAnime=true`.

### ✅ FIXED — v1.2.7: Formato de streams 100% unificado (2026-06-12)
- **BUG:** `normalizeStream()` preservaba el formato original de Pigamer37 (con `"Source\nServer"`), causando que anime y otros providers mostraran información diferente.
- **Fix:** Ahora TODOS los streams se normalizan al mismo formato sin excepción.
  - `name`: `"ProviderName\nQuality 🇯🇵🇪🇸"` — nombre del provider, resolución y banderas
  - `title`: `"Quality | ProviderName\nServer\nFlags\nEpisode"` — info detallada
- Se extrae calidad desde `stream.quality`, título original o nombre. Se extraen banderas de idioma desde el título original.
- Providers diferenciados por etiqueta: `"Pigamer37: AnimeFLV"`, `"Alfa: SourceName"`, `"2embed (Vesy)"`, etc.

### ✅ FIXED — v1.3.4: Alfa provider label ahora muestra página real (2026-06-12)
- **FIXED:** `normalizeStream` — Alfa providers ahora muestran `"Alfa: Cuevana2"` en vez de genérico `"Alfa Providers"`
- **Cambio:** se eliminó el bypass `providerId !== 'alfa-providers'` y se agregó lógica `isAlfa` con formato `"Alfa: ${sourceName}"`
- **AFECTA:** Películas, series, anime, dramas — cualquier stream de Alfa ahora muestra el nombre real del canal (Cuevana2, PelisPedia, AnimeFLV, Gnula, etc.)
- **VERSION:** bumped to 1.3.4

### ✅ FIXED — v1.3.4b: NoTorrent label muestra el source real (2026-06-12)
- **FIXED:** `normalizeStream` — streams de NoTorrent ya no muestran la etiqueta genérica `"NoTorrent"`.
- **Cambio:** si `sourceName` es genérico (igual al `providerName` o `"NoTorrent"`) y la URL tiene un servidor conocido (vía `SERVER_MAP` o `?url=` param), se usa ese nombre como `effectiveSource`.
- **Ejemplos:** `notorrent2.workers.dev/?url=...321moviesfree.com` → `"321MoviesFree"`, `mega.nz/file/...` → `"Mega"`
- **Fallback:** si no se detecta un source real, conserva el label original (`"NoTorrent"`).
- **AFECTA:** Todos los streams de NoTorrent que pasan por un proxy con URL detectable.

### ✅ FIXED — v1.3.4c: Eliminado nombre "Pigamer37" de labels + flags duplicados + detección anime vía TMDB (2026-06-12)
- **FIXED:** Labels de anime ya no muestran `"Pigamer37: AnimeFLV"`, ahora solo `"AnimeFLV"`.
- **FIXED:** Flags de idioma ya no se duplican al final del `title` — solo aparecen en `name` junto a la calidad.
- **FIXED:** Series con género Animation (TMDB genre ID 16) ahora se detectan como anime aunque el ID no tenga prefijo anime (`tmdb:12345` en vez de `animeflv:slug`). Esto activa Pigamer37 proxy + Alfa anime providers para estas series.
- **CAMBIO:** Nueva lógica `isAnime` en stream handler: primero verifica `isAnimeId(id)`, si es false pero `enableAnime && mediaType === 'tv' && rawId` es numérico, consulta TMDB para ver si tiene genre 16 (Animation).

---

## Next steps

1. [x] **FEATURE:** Integrar providers server-side (Alfa + Hermes scrapers via require)
2. [x] **FEATURE:** Implementar deduplicación de streams (dedupeStreams)
3. [x] **BUG:** Corregir genre format (id:name → parseGenreId)
4. [x] **BUG:** Re-agregar tipo 'other' al manifest
5. [x] **BUG:** Refactor catalog/meta handlers para usar isTypeEnabled
6. [x] **BUG:** Limpiar double console.log
7. [x] **DEPLOY:** Commit + push a `main` para que los fixes lleguen a Render
8. [x] **VERIFICADO:** The Matrix → streams Alfa funcionales desde Render
9. [x] **VERIFICADO:** Render produce streams Alfa (CineCalidad, PelisPedia, HomeCine funcionando)
10. [x] **FEATURE:** Catálogos universales con IDs `tt:<imdb_id>` para compatibilidad cross-addon
11. [x] **FEATURE:** Embed resolver (`tryResolveEmbedToDirect`) con fetch y extracción de .m3u8/.mp4
12. [x] **FEATURE:** notWebReady automático — streams directos marcados como ExoPlayer-compatibles
13. [x] **BUG:** Fix stremio:// URL (doble protocolo)
14. [x] **FEATURE:** POST search support en searchProvider
15. [x] **FEATURE:** Torrent indexers server-side — GloDLS (movies/TV) + Nyaa.si (anime)
16. [x] **FEATURE:** Embed resolver domain-specific (YouTube, JWPlayer via embed-resolver.js)
17. [x] **AUDIT:** Hermes (62) + backend (4) scrapers — TODOS rotos. Replaced by torrent indexers
18. [ ] Probar integracion completa en Android TV / NuvioTV
19. [ ] Verificar GloDLS desde Render (posible bloqueo IP)
20. [ ] Evaluar si hay providers Alfa cuyos embeds puedan resolverse (JWPlayer con config inline)
21. [ ] Tests automatizados — Suite que verifique estructura válida de cada scraper
