# Ovnivers — Project Memory

## What is this
Addon para **Nuvio 0.7.5** que combina 61 scrapers de streaming de [All-in-One-Nuvio](https://github.com/D3adlyRocket/All-in-One-Nuvio) adaptados al protocolo Stremio con backend en **Render.com**. Proxy de anime integrado via Pigamer37 (AnimeFLV/AnimeAV1/TioAnime/Henaojara).

---

## Architecture (v1.2.5)

```
Nuvio App (0.7.5)
  |-- Settings > Addons
  |     URL: https://ovnivers.onrender.com/manifest.json
  |     URL configurada: .../?configured=<base64>
  |
  |-- Backend: Render.com (Node.js / Express)
  |     |-- /manifest.json           → Stremio manifest (respeta config)
  |     |-- /stream/:type/:id.json   → Backend scrapers (4) + Alfa providers
  |     |                              + local providers (server-side) + Pigamer37 proxy
  |     |-- /catalog/:type/:id.json  → 18 catalogs (TMDb + anime)
  |     |-- /meta/:type/:id.json     → TMDb metadata + anime metadata
  |     |-- /configure               → Panel moderno con generate URL
  |     |-- /                        → Health check
  |
  |-- Server-side provider execution (Nuevo en v1.2.0+):
  |     |-- Alfa providers (80+ canales) se ejecutan en el servidor via require()
  |     |-- 61 scrapers Hermes se intentan cargar; ~61 funcionales en Node.js
  |     |-- Deduplicación integrada: dedupeStreams() elimina duplicados por url/name
  |     |-- Límite: MAX_STREAM_RESULTS=80 por request
  |     |-- Timeout: LOCAL_PROVIDER_TIMEOUT=12s por provider
  |     |-- Concurrencia: LOCAL_PROVIDER_CONCURRENCY=8 providers en paralelo
  |
  |-- Modo legacy (deshabilitado por defecto):
        exposeLocalScrapers=false → el campo "scrapers" NO se incluye en manifest
        Si se activa, Nuvio los descarga y ejecuta en Hermes (motor JS local).
```

---

## Key discoveries

### 1. Los scrapers ofuscados son para Hermes, NO para Node.js
- Los 61 archivos `.js` en `providers/` estan ofuscados con `__async`, `__defProp`, etc.
- Diseniados para **Hermes** (motor JS de React Native en Nuvio).
- **NO se pueden ejecutar en el backend.** Son para ejecucion local en el dispositivo.

### 2. Nuvio 0.7.5 usa protocolo Stremio
- Solo existe "Addons" (Stremio protocol), no "Local Scrapers".
- Campos obligatorios: `id`, `version`, `name`, `logo`, `resources`, `types`, `idPrefixes`.
- Formato de resources: array de objetos `[{name, types, idPrefixes}]`.

### 3. El campo "scrapers" en el manifest
- Si se incluye, Nuvio los ejecuta **localmente en el dispositivo**.
- Se puede omitir via config (`enableLocal: false`).
- Se envia condicionalmente en el manifest segun `?configured=`.

### 4. APIs de streaming bloquean IPs de servidores (INVESTIGADO 2026-06-11)

**Resultado del test masivo:** practicamente NINGUNA API de movies/TV devuelve JSON desde Render.

| Provider | URL | Resultado |
|---|---|---|
| streamsrcs.2embed.cc (vesy) | `/vesy?tmdb=550` | HTML (pagina player, no JSON) |
| streamsrcs.2embed.cc (vsrc) | `/vsrc?imdb=tt0137523` | HTML |
| www.2embed.skin | `/api/movie/550.json` | null (bloqueado) |
| vidsrc.rip | `/api/movie/550` | fetch failed |
| vidsrc.to | `/api/v2/movie/550` | 404 |
| vidsrc.pro | `/api/movie/550` | aborted |
| vidsrc.cc | `/api/movie/550` | aborted |
| vidsrc.stream | `/api/movie/550` | fetch failed |
| vidsrc.net | `/api/movie/550` | fetch failed |
| vidsrc.icu | `/api/movie/550` | fetch failed |
| vidsrc.xyz | `/api/movie/550` | fetch failed |
| vidsrcme | `/api/movie/550` | fetch failed |
| vidlink.pro | `/api/movie/550` | 403 |
| embed.su | `/api/movie/550` | fetch failed |
| vidcloud.icu | `/api/movie/550` | 404 |
| superembed | `/api/movie/550` | fetch failed |
| vidbinge.dev | `/api/movie/550` | HTML placeholder (no JSON) |
| moviesapi.club | `/movie/550` | fetch failed |
| multiembed.mov | `/api/movie/550` | 404 |
| autoembed.co | `/api/movie/550` | 404 |
| vidplay.site | `/api/movie/550` | 404 |
| player.videasy.net | `/api/movie/550` | 404 |

**Conclusion:** TODAS las APIs de peliculas/series estan muertas o bloqueadas desde servidores. 2embed.cc devuelve HTML con proteccion anti-devtool, el stream real requiere seguir una cadena de iframes.

**Workaround (v1.2.0):** `parse2embedHTML()` extrae m3u8/mp4/iframe/data-src URLs del HTML de 2embed.cc mediante 6 patrones regex. Funciona parcialmente.

**Unico que funciona:** `pigamer37.alwaysdata.net` para anime (alwaysdata.net no bloquea IPs). TMDB API tambien siempre funciona.

### 5. Pigamer37 como proxy de anime
- El addon original de Pigamer37 usa `|` (pipe) en IDs de catalogo: `animeflv|onair`, `animeflv|search`
- Content IDs usan `:` (colon): `animeflv:slug`, `animeav1:slug`
- Soporta: AnimeFLV, AnimeAV1, TioAnime, Henaojara
- IDs: `animeflv:`, `animeav1:`, `henaojara:`, `tioanime:`, `anilist:`, `kitsu:`, `mal:`, `anidb:`
- **IMPORTANTE:** Pigamer37 espera IDs con `%7C` (pipe encodeado), NO `%3A` (colon encodeado). `encodeURIComponent` encodea `:` como `%3A` — hay que usar `fixPigamerId()` que reemplaza `:` por `%7C`.

### 6. Stremio stream format (Pigamer37 reference)
- `name`: `"SourceName\nServerName"` (multilinea con `\n`)
- `title`: `"Episode Title\n⚙️ ServerName\n🔗 url\n🇯🇵🇪🇸"` (info detallada)
- `behaviorHints.notWebReady: true` para streams internos
- `behaviorHints.bingeGroup`: `"source|server"` para autoplay
- `externalUrl` (sin `url`) para streams que abren en navegador
- `proxyHeaders` para headers custom al fetchear el video

### 7. Stremio catalog format (Pigamer37 reference)
- Catalogs con `extra: [{ name: "genre", options: [...], optionsLimit: 1 }]` para generos
- `extra: [{ name: "search", isRequired: true }]` para busqueda
- `skip` para paginacion: `page = Math.floor(skip / pageSize) + 1`
- Genre catalog sin search: `id: "animeflv|genres"`, `extra.genre.isRequired: true`
- Los `idPrefixes` deben incluir todos los prefixes usados en items (incluyendo `tmdb-genre:`)

---

## Current state (v1.2.6)

- **Manifest:** Stremio protocol v4 + catalog + meta + stream. `scrapers` omitido por defecto (exposeLocalScrapers=false).
- **Backend:** 4 scrapers activos (2embed Vesy, 2embed Vsrc, PoseidonHD). VidSrc parcialmente funcional. EZTV y Cuevana2 removidos.
- **2embed HTML parser:** Regex que extrae m3u8/mp4/iframe/data-src desde paginas HTML de 2embed.cc.
- **Server-side providers:** Alfa providers (80+ canales) + ~61 scrapers Hermes se ejecutan en el servidor via `enableLocal`. Timeout 12s por provider, 8 en paralelo, max 80 resultados.
- **Deduplicación:** `dedupeStreams()` elimina duplicados por url/name/infoHash antes de cachear.
- **Stream format unificado:** `normalizeStream()` preserva streams que ya tienen formato multiline (Pigerman37: `"Source\nServer"`, Alfa: `"Provider\nServer"`). Solo formatea streams simples (backend) con `"ProviderName\nQuality"`.
- **Anime merge:** Pigamer37 + Alfa (categoría `anime`) + providers locales (con TMDB ID resuelto via Pigamer37 meta) se ejecutan en paralelo. Los streams de Pigamer37 conservan su formato original (episodio, servidor, flags de idioma).
- **Pigamer37 query string:** Reconstruida desde `req.query` (season/episode) en vez de `req.url` (roto por catch-all route).
- **Local scrapers:** 61 originales (cargables en Node.js) + **alfa-providers** (53/77 activos server-side).
- **Catalogs:** 19 catalogs (Popular, Trending, Top Rated, By Genre, By Year + 8 anime + **All Sources Search**). Genre format usa `id: name` con `parseGenreId()`.
- **All Sources Search:** `all-search` catalog que busca en TMDB + AnimeFLV + AnimeAV1 + TioAnime + Henaojara simultáneamente, resultados combinados sin duplicados.
- **Meta:** TMDb + anime providers (cross-reference IDs resuelto).
- **Configure:** Panel moderno dark-theme con generate URL, filtro de idiomas, calidad, tipos.
- **Dependencias:** `cheerio@latest`, `crypto-js`, `esbuild@^0.25.0`, `express@^4.21.0`. 0 vulnerabilidades.
- **Render:** Auto-deploy desde `main`. Cold start ~30s.
- **GitHub:** https://github.com/leonidas10009/ovnivers

### Test results (2026-06-12 — desde Render)

| Categoria | Accesibles | Bloqueados |
|---|---|---|
| Backend scrapers | 3/4 activos | VidSrc parcial |
| Alfa Movies | 28/43 (65%) | 15 bloqueados |
| Alfa Series | 5/6 (83%) | 1 bloqueado |
| Alfa Anime | 14/22 (64%) | 8 bloqueados |
| Alfa Documentales | 3/4 (75%) | 1 bloqueado |
| **Total alfa providers** | **53/77 (69%)** | **24 bloqueados** |

---

## Files structure

```
ovnivers/
  server.js            → Express backend (main for Render), 1262 líneas
    manifest.json        → Stremio manifest + 62 scrapers (v1.2.5)
  package.json         → cheerio, crypto-js, esbuild, express
  render.yaml          → Render deploy config
  railway.json         → Railway deploy config (alternative)
  build.js             → esbuild bundler
  providers/           → 62 .js scrapers (Hermes, cargables en Node.js)
  src/alfa-providers/  → Source for alfa-providers scraper
    index.js           → Main entry: resolveTitle + scrapeAlfaProviders
    engine.js          → fetchHTML, searchProvider, extractVideos, detectServer
    providers.js       → 80+ provider configs (URLs, selectors, extraction types)
  logo.png             → Addon icon
  .gitignore           → Excludes node_modules, .memory.md
  .memory.md           → This file
  README.md            → Project documentation

Nota: providers/ se cargan en server.js via require() para ejecución server-side.
Los scrapers Hermes que fallan al cargar se omiten silenciosamente (try/catch).
```

---

## Dependencies

| Package | Version | Purpose |
|---|---|---|
| express | ^4.21.0 | HTTP server, Stremio protocol |
| cheerio | ^1.2.0 | HTML parsing |
| cheerio-without-node-native | npm:cheerio@^1.2.0 | Alias for Hermes scrapers |
| crypto-js | ^4.2.0 | AES/SHA decryption |
| esbuild | ^0.25.0 | Bundler for local scrapers |

---

## Deployment

### Render.com
- **Service:** ovnivers (Web Service)
- **Plan:** Free (512MB RAM, cold start ~30s)
- **Repo:** leonidas10009/ovnivers
- **Build:** `npm install` → `npm run build`
- **Start:** `node server.js`
- **URL:** https://ovnivers.onrender.com
- **Auto-deploy:** enabled (pushes to `main`)

---

## URLs

| Resource | URL |
|---|---|
| GitHub repo | https://github.com/leonidas10009/ovnivers |
| Render backend | https://ovnivers.onrender.com |
| Manifest | https://ovnivers.onrender.com/manifest.json |
| Configure | https://ovnivers.onrender.com/configure |

---

## Config protocol

El panel `/configure` genera una URL con los ajustes codificados en base64:

```
https://ovnivers.onrender.com/manifest.json?configured=<json_base64>
```

El backend decodifica `configured` y:
1. Filtra `catalogs`, `resources`, `types`, `idPrefixes` en el manifest
2. Omite `scrapers` si `enableLocal: false`
3. Filtra streams por `quality` en `/stream`
4. Bloquea requests de tipos deshabilitados

Campos configurables:
```json
{
  "enableMovies": true,
  "enableSeries": true,
  "enableAnime": true,
  "quality": "all",
  "langs": ["en", "es", "ja", ...],
  "enableBackend": true,
  "enableLocal": true
}
```

---

## Known issues (audit 2026-06-12)

### ✅ FIXED — alfa-providers no funciona para anime ni documentales
- **Fix aplicado:** filtro cambiado a `p.categories.includes(category)` con mapeo `movie→movie, series→tvshow, anime→anime, other→documentary`

### ✅ FIXED — Anime IDs no se resuelven en alfa-providers
- **Fix aplicado:** para IDs con prefix anime, extraer slug directo sin pasar por TMDB

### ✅ FIXED — Pigamer37 IDs encodeados mal
- **Fix aplicado:** `fixPigamerId(id)` usado en stream, catalog y meta

### ✅ FIXED — Backend Cuevana2/PoseidonHD: `s + 0.2` no asigna
- **Fix aplicado:** cambiado a `s += 0.2`

### ✅ FIXED — Catalog genre items usan `tmdb-genre:` sin declarar en idPrefixes
- **Fix aplicado:** `"tmdb-genre:"` agregado a manifest y server

### ✅ FIXED — `type === 'anime'` fallback demasiado amplio
- **Fix aplicado:** solo `if (isAnimeId(id))` sin el `|| type === 'anime'`

### ✅ FIXED — Cache sin limites de tamaño
- **Fix aplicado:** LRU eviction con max 1000 entradas

### ✅ FIXED — TMDB API key hardcodeada
- **Fix aplicado:** `process.env.TMDB_KEY` con fallback

### ✅ FIXED — 24 providers muertos marcados inactive
- **Fix aplicado:** test masivo desde Render, 24 bloqueados marcados `active: false`

### ✅ FIXED — Backend EZTV y Cuevana2 removidos
- **Fix aplicado:** no funcionan desde Render (403/fetch failed), quitados del array activo

### ✅ FIXED — 2embed devuelve HTML, no JSON → scrapers no extraen streams (2026-06-12)
- **Fix aplicado:** `parse2embedHTML()` extrae m3u8/mp4/iframe/data-src/json-block URLs via regex
- `scrape2embedVesy` y `scrape2embedVsrc` ahora detectan `typeof data === 'string'` y usan el parser HTML
- 6 patrones regex: m3u8, mp4, iframe src, JS assignments, data-src attributes, JSON blocks

### ✅ FIXED — Stremio manda extra params en URL path, no en query string (2026-06-12)
- **Fix aplicado:** rutas catch-all `/:type/:id/:extra(*).json` para stream, catalog y meta
- `parsePathExtras()` convierte `genre=28&skip=20` del path en `req.query`
- Handlers refactorizados a funciones `handleStream()`, `handleCatalog()`, `handleMeta()`

### ✅ FIXED — Anime catalogs usan type incorrecto (2026-06-12)
- **Fix aplicado:** todos los catalogs de anime cambiados a `type: "series"` (manifest.json + server.js)
- Coincide con Pigamer37 y Stremio espera `series` para contenido episodico

### ✅ FIXED — Genre catalogs no tienen `options` en manifest extra (2026-06-12)
- **Fix aplicado:** `loadGenres()` carga generos TMDB (movie + tv) con cache 24h
- Manifest genera `extra: [{ name: "genre", options: [...], optionsLimit: 1 }]` dinamicamente
- Fallback estatico en manifest.json con lista completa de generos

### ✅ FIXED — El addon no aparece como provider en catalogs de otros addons (2026-06-12)
- **Fix aplicado:** 2embed scrapers ahora devuelven streams reales (HTML parser)
- Manifest ya tenia `tt` en stream idPrefixes — el problema era que los scrapers no devolvian nada

### ✅ FIXED — Catalogo "By Year" no funciona sin search param (2026-06-12)
- **Fix aplicado:** `isRequired: true` en manifest + fallback a `new Date().getFullYear()` en server

### ✅ FIXED — `fixPigamerType()` y `cheerio` fallback son codigo muerto (2026-06-12)
- **Fix aplicado:** `fixPigamerType()` eliminado. `scrapeEZTV`, `scrapeCuevana2Espanol`, `getTitle` eliminados
- `scrapePoseidonHD` refactorizado para usar `getTMDbMeta()` directamente

### ✅ FIXED — Pigamer37 double-encoding bug (2026-06-12)
- **Fix aplicado:** `fixPigamerId()` ahora retorna `|` literal (no `%7C`), `encodeURIComponent` lo convierte a `%7C`
- Antes retornaba `%7C` literal → `encodeURIComponent` encodeaba `%` → `%257C` (doble encoding)

### ✅ FIXED — Pigamer37 ID encoding: `:` vs `|` confusion (2026-06-12, v1.2.4)
- **BUG RAÍZ de todo el fallo de anime:** `fixPigamerId()` convertía `:` → `|` para TODOS los IDs
- **Realidad:** Pigamer37 usa `|` solo en catalog IDs (`animeflv|onair`), pero `:` en stream/meta IDs (`animeflv:one-piece`)
- `encodeURIComponent` encodea `:` como `%3A` y `|` como `%7C`
- Ovnivers enviaba `animeflv%7Cone-piece` → Pigamer37 devolvía `{"streams":[],"message":"Failed getting Anime info"}`
- **Fix:** `fixPigamerId()` ahora es no-op (`return id`). `encodeURIComponent` maneja la codificación correcta automáticamente
- Catalog IDs con `|` se encodean a `%7C` correctamente. Stream/meta IDs con `:` se encodean a `%3A` correctamente
- **Verificado:** `animeflv%3Aone-piece` → Pigamer37 devuelve 30+ streams reales

### ✅ FIXED — Anime catalog detection con IDs sin colon (2026-06-12)
- **Fix aplicado:** `isAnimeId(cleanId + ':')` en handleCatalog para IDs como `animeflv` (sin `:`)

### ✅ FIXED — Anime cross-reference IDs no se resuelven (2026-06-12)
- **Fix aplicado:** `resolveAnimeId()` consulta `/meta` de Pigamer37 para convertir `kitsu:`, `mal:`, `anilist:`, `anidb:` a IDs fuente (`animeflv:`, etc.)
- Stream handler ahora resuelve IDs antes de proxy a Pigamer37
- Esto permite que el addon aparezca como provider cuando otros addons (Anime Kitsu) devuelven IDs cross-reference

### ✅ FIXED — idPrefixes incorrectos por recurso (2026-06-12)
- **Fix aplicado:** `stream` ya no incluye `tmdb-genre:` (no tiene sentido para streams)
- `catalog` ya no incluye `tt` (no tiene sentido para catalogs)
- Cada recurso tiene sus idPrefixes especificos

### ✅ FIXED — Compatibilidad con Stremio (2026-06-12)
- **Fix aplicado:** `types` incluye `"other"` para compatibilidad con addons que usan ese tipo
- `behaviorHints.newEpisodeNotifications: true` para notificaciones de episodios
- `idPrefixes` por recurso limpios y correctos

### ✅ ADDED — Server-side provider execution + deduplication (2026-06-12)
- **Integración providers:** Alfa providers + scrapers Hermes cargados via `require()` y ejecutados en el servidor
- **Deduplicación:** `dedupeStreams()` elimina streams duplicados por url/name/infoHash antes de cachear
- **Helpers nuevos:** `normalizeStream()`, `withTimeout()`, `runInChunks()`, `scrapeLocalProviders()`, `scrapeAlfa()`
- **Config keys nuevas:** `enableCatalogs` (default: true), `exposeLocalScrapers` (default: false)
- **ID parsing:** `parseStreamId()` extrae contentId/season/episode de IDs `tmdb:123:1:1` o `tt123:1:1`
- **Genre format:** Cambiado a `"id: name"` (ej. `"28: Action"`) con `parseGenreId()` que extrae el ID numérico
- **Límites configurables:** `MAX_STREAM_RESULTS=80`, `LOCAL_PROVIDER_TIMEOUT=12s`, `LOCAL_PROVIDER_CONCURRENCY=8`

### ✅ FIXED — Codex issues corregidos (2026-06-12)
- **Genre format:** `parseGenreId()` ahora se usa en `handleCatalog` para extraer el ID numérico del formato `"28: Action"`
- **Tipo 'other':** Re-agregado a `enabledTypes` en manifest (se había eliminado en la refactorización)
- **handleCatalog:** Refactorizado para usar `isTypeEnabled()` y `parseGenreId()`
- **handleMeta:** Refactorizado para usar `isTypeEnabled()`
- **Console.log:** Limpiado double log en handleStream

### ✅ FIXED — v1.2.6: Anime Alfa/Local providers no aparecían (2026-06-12)
- **BUG RAÍZ:** `scrapeAlfa()` se llamaba con `type='series'` → `mapTypeToCategory('series')` devolvía `'tvshow'`, pero los providers de anime están en categoría `'anime'`. Alfa providers buscaban en categoría incorrecta y devolvían vacío.
- **Fix:** `scrapeAlfa()` ahora recibe `type='anime'` para anime IDs, buscando en la categoría correcta.
- **BUG RAÍZ 2:** `scrapeLocalProviders()` recibía `rawId='animeflv:slug'` que no es un TMDB ID válido, los scrapers Hermes no encontraban nada.
- **Fix:** Nueva función `getAnimeTMDbId()` que consulta `/meta/series/` de Pigamer37 para resolver un TMDB ID. Si lo encuentra, lo pasa a `scrapeLocalProviders()`.

### ✅ FIXED — v1.2.6: Stream format inconsistente (2026-06-12)
- **BUG:** `normalizeStream()` sobrescribía `title` de Pigamer37 con `"Quality\nPigamer37"`, perdiendo episodio, servidor y flags de idioma.
- **Fix:** streams con `name` multiline se preservan intactos (incluyendo su `title` original). Solo streams con `name` simple se formatean a `"Provider\nQuality"`.

### ✅ ADDED — v1.2.6: Combined search catalog (2026-06-12)
- **FEATURE:** Nuevo catálogo `all-search` (type: series) que busca simultáneamente en TMDB + AnimeFLV + AnimeAV1 + TioAnime + Henaojara.
- Resultados mergeados y deduplicados por nombre, top 50.
- Respeta config: usa TMDB si `enableBackend=true`, anime sources si `enableAnime=true`.

---

## Next steps (post-codex 2026-06-12)

1. [x] **FEATURE:** Integrar providers server-side (Alfa + Hermes scrapers via require)
2. [x] **FEATURE:** Implementar deduplicación de streams (dedupeStreams)
3. [x] **BUG:** Corregir genre format (id:name → parseGenreId)
4. [x] **BUG:** Re-agregar tipo 'other' al manifest
5. [x] **BUG:** Refactor catalog/meta handlers para usar isTypeEnabled
6. [x] **BUG:** Limpiar double console.log
7. [ ] Probar integracion completa en Android TV
8. [ ] Verificar que 2embed HTML parser extrae streams reales en produccion
9. [ ] Agregar toggles `enableCatalogs` y `exposeLocalScrapers` al panel /configure
10. [ ] Monitorear performance de 61 providers server-side desde Render (free plan)
11. [ ] Verificar que Alfa providers + locales aparecen en streams de anime en produccion
12. [ ] Verificar que `all-search` catalog funciona con Stremio
